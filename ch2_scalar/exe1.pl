# !/usr/bin/perl
use strict;
use warnings;
use Math::Trig;

my $radius = 12.5;
my $pi = pi;

my $circumference = $radius * $pi * 2;

print "the circumference of circle which ahs raidus $radius is $circumference\n";
